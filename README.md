# Digital-Clock
**Digital Clock using JavaScript**  A simple digital clock built with HTML, CSS, and JavaScript that displays the current time and updates every second. This project uses the JavaScript `Date()` object and `setInterval()` to dynamically update the clock in real time. It demonstrates basic DOM manipulation and time-based functions.
